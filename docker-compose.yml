services:
  vipp:
    image: elenapost/vipp:latest
    container_name: vipp
    restart: always
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      ASPNETCORE_HTTP_PORTS: 8080
      ASPNETCORE_HTTPS_PORTS: 8081
